This script uses a conditional structure to determine if the input is a file or a directory. It utilizes the wc command to extract file statistics and ls combined with wc to count the number of files and .txt files within a directory.
